specVersion: "v1beta"
serviceId: "wingscafe"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "wingscafe-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
